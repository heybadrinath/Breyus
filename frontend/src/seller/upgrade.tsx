import React from "react";
import {Layout } from "./components";

const UpgradeBody = () =>{
    return(
        <div></div>
    );
};

const Upgrade = () =>{
    return(
        <Layout Body={<UpgradeBody/>}/>
    );
};

export default Upgrade;