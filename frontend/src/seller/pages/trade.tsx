import React from "react";
import {SearchHeaderLight  } from "../../components/Header"

export const Trade = () => {
    return (
        <div>
            <SearchHeaderLight />
        </div>
    );
}