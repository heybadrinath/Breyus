[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "breyus-ai-pipeline"
version = "0.0.0"
description = "CLI wrapper for the Breyus AI data pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pipeline = "pipeline.scripts.cli:main"
